/*
Esto se hace ya que se necesita ejecutar primero dotenv para obtener las variables de entorno
y si lo ponemos en el index no lo corre hasta que lo necesite asi que aqui lo ejecutamos antes de index.js
*/

import dotenv from 'dotenv'
dotenv.config();